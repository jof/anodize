//! Legacy operation types — retained only for compilation during migration.
//! All ceremony operations now use the script engine in `ceremony/scripts/`.
//!
//! TODO: Remove this module entirely once all callers in `app.rs` and
//! `dispatch.rs` are cleaned up.

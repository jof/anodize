target/
# Cargo.lock is committed for binary crates (required for reproducible Nix builds).
.claude/
# Nix build result symlinks
result
result-*
# Build artifacts (produced by make targets)
anodize.iso
fake-usb.img

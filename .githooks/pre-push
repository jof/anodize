#!/usr/bin/env bash
set -euo pipefail

branch="$(git symbolic-ref --short HEAD 2>/dev/null || true)"

if [ "$branch" = "jira" ]; then
  echo "ERROR: pushing the 'jira' branch is not allowed." >&2
  exit 1
fi

#!/usr/bin/env bash
set -euo pipefail

if ! command -v cargo &>/dev/null; then
  echo "cargo not found; skipping fmt check" >&2
  exit 0
fi

echo "Running cargo fmt --check..."
cargo fmt --all -- --check
